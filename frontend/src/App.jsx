import { useMemo, useState } from 'react'

const seedDeliveries = [
  { id: 'RFX-1042', customerName: 'Amina Wanjiku', phone: '0712 345 678', address: 'Westlands, Raphta Road, Nairobi', itemDescription: '2 × wireless headphones', status: 'PICKED_UP', createdAt: 'Today, 9:15 AM', timeline: [['Delivery request created', 'Today, 9:15 AM'], ['Assigned to Peter Mwangi', 'Today, 9:28 AM'], ['Package picked up', 'Today, 10:05 AM']] },
  { id: 'RFX-1041', customerName: 'David Otieno', phone: '0798 521 330', address: 'Kilimani, Wood Avenue, Nairobi', itemDescription: 'Office chair — black', status: 'ASSIGNED', createdAt: 'Today, 8:42 AM', timeline: [['Delivery request created', 'Today, 8:42 AM'], ['Assigned to Amina Njeri', 'Today, 8:58 AM']] },
  { id: 'RFX-1039', customerName: 'Grace Muthoni', phone: '0701 678 112', address: 'Lavington, Muthangari Drive, Nairobi', itemDescription: 'Skincare gift set', status: 'DELIVERED', createdAt: 'Yesterday, 3:20 PM', timeline: [['Delivery request created', 'Yesterday, 3:20 PM'], ['Assigned to Peter Mwangi', 'Yesterday, 3:34 PM'], ['Package picked up', 'Yesterday, 4:06 PM'], ['Delivered — confirmation recorded', 'Yesterday, 5:18 PM']] },
  { id: 'RFX-1037', customerName: 'Brian Kariuki', phone: '0722 119 840', address: 'Parklands, 6th Avenue, Nairobi', itemDescription: 'Printer ink cartridges', status: 'DELIVERY_FAILED', createdAt: 'Yesterday, 11:05 AM', failureReason: 'Customer unavailable', timeline: [['Delivery request created', 'Yesterday, 11:05 AM'], ['Assigned to Amina Njeri', 'Yesterday, 11:22 AM'], ['Package picked up', 'Yesterday, 12:00 PM'], ['Delivery failed — customer unavailable', 'Yesterday, 12:40 PM']] },
]

const styles = { NEW: 'bg-sky-50 text-sky-700 ring-sky-600/20', ASSIGNED: 'bg-violet-50 text-violet-700 ring-violet-600/20', PICKED_UP: 'bg-amber-50 text-amber-700 ring-amber-600/20', DELIVERED: 'bg-emerald-50 text-emerald-700 ring-emerald-600/20', DELIVERY_FAILED: 'bg-rose-50 text-rose-700 ring-rose-600/20' }
const pretty = (s) => s.replaceAll('_', ' ').toLowerCase().replace(/\b\w/g, (x) => x.toUpperCase())
function Status({ value }) { return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ring-1 ring-inset ${styles[value]}`}>{pretty(value)}</span> }

function RolePicker({ onChoose }) {
  const roles = [['retailer', 'Retailer', 'Create and follow up on every customer delivery.', '▣', 'bg-blue-600'], ['dispatcher', 'Dispatcher', 'Assign open deliveries and manage your riders.', '⌘', 'bg-violet-600'], ['rider', 'Rider', 'View jobs and update delivery progress.', '↗', 'bg-emerald-600']]
  return <main className="min-h-screen bg-slate-950 px-5 py-10 text-white sm:flex sm:items-center sm:justify-center"><div className="w-full max-w-5xl"><div className="mb-12 text-center"><div className="mx-auto mb-6 grid h-12 w-12 place-items-center rounded-2xl bg-blue-500 text-2xl font-black shadow-lg shadow-blue-500/30">R</div><p className="mb-3 text-sm font-semibold uppercase tracking-[0.24em] text-blue-300">Delivery coordination, simplified</p><h1 className="text-4xl font-bold tracking-tight sm:text-5xl">Welcome to Reflex</h1><p className="mx-auto mt-4 max-w-xl text-base leading-7 text-slate-400">Choose your workspace to get started. Every delivery stays visible from request to confirmation.</p></div><div className="grid gap-5 md:grid-cols-3">{roles.map(([key, title, description, icon, color]) => <button key={key} onClick={() => onChoose(key)} className="group rounded-3xl border border-slate-800 bg-slate-900 p-7 text-left transition hover:-translate-y-1 hover:border-slate-600 hover:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-blue-400"><span className={`mb-12 grid h-12 w-12 place-items-center rounded-2xl text-xl font-bold ${color}`}>{icon}</span><h2 className="text-xl font-bold">{title}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-slate-400">{description}</p><span className="mt-6 inline-block text-sm font-semibold text-blue-300 group-hover:text-white">Open workspace →</span></button>)}</div></div></main>
}

function App() {
  const [role, setRole] = useState(null)
  const [screen, setScreen] = useState('deliveries')
  const [deliveries, setDeliveries] = useState(seedDeliveries)
  const [selected, setSelected] = useState(seedDeliveries[0])
  const [query, setQuery] = useState('')
  const [filter, setFilter] = useState('ALL')
  const [notice, setNotice] = useState('')
  const [form, setForm] = useState({ customerName: '', phone: '', address: '', itemDescription: '' })
  const visible = useMemo(() => deliveries.filter((d) => (filter === 'ALL' || d.status === filter) && (!query || [d.id, d.customerName, d.address].some((v) => v.toLowerCase().includes(query.toLowerCase())))), [deliveries, filter, query])
  const active = deliveries.filter((d) => ['NEW', 'ASSIGNED', 'PICKED_UP'].includes(d.status)).length
  const delivered = deliveries.filter((d) => d.status === 'DELIVERED').length
  const attention = deliveries.filter((d) => d.status === 'DELIVERY_FAILED').length
  const create = (e) => { e.preventDefault(); const time = new Date().toLocaleTimeString('en-KE', { hour: 'numeric', minute: '2-digit' }); const d = { id: `RFX-${1043 + deliveries.length}`, ...form, status: 'NEW', createdAt: `Today, ${time}`, timeline: [['Delivery request created', `Today, ${time}`]] }; setDeliveries((all) => [d, ...all]); setSelected(d); setForm({ customerName: '', phone: '', address: '', itemDescription: '' }); setScreen('deliveries'); setNotice(`${d.id} was created and is ready for assignment.`) }
  if (!role) return <RolePicker onChoose={setRole} />
  if (role !== 'retailer') return <main className="grid min-h-screen place-items-center bg-slate-50 px-5 text-center"><div className="max-w-md rounded-3xl bg-white p-10 shadow-sm ring-1 ring-slate-200"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-xl">R</div><h1 className="mt-5 text-2xl font-bold">{pretty(role)} workspace</h1><p className="mt-3 leading-6 text-slate-500">This demo currently focuses on the retailer workflow. Select Retailer to create and track deliveries.</p><button onClick={() => setRole(null)} className="mt-7 rounded-xl bg-slate-900 px-5 py-3 text-sm font-semibold text-white">Choose another role</button></div></main>
  return <div className="min-h-screen bg-slate-50 text-slate-900"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><button onClick={() => setRole(null)} className="flex items-center gap-3 text-left"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 font-black text-white">R</span><span><span className="block font-bold">Reflex</span><span className="block text-xs text-slate-500">Retailer workspace</span></span></button><button onClick={() => setRole(null)} className="rounded-lg px-3 py-2 text-sm font-medium text-slate-600 hover:bg-slate-100">Switch role</button></div></header><main className="mx-auto max-w-7xl px-5 py-8"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-blue-600">GOOD MORNING, MERCHANT</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Your deliveries</h1><p className="mt-2 text-slate-500">Create, monitor, and resolve orders from one place.</p></div><button onClick={() => setScreen(screen === 'create' ? 'deliveries' : 'create')} className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white shadow-sm shadow-blue-600/25 hover:bg-blue-700">{screen === 'create' ? '← Back to deliveries' : '+ New delivery'}</button></div>{notice && <div className="mb-6 flex items-center justify-between rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800"><span>✓ {notice}</span><button onClick={() => setNotice('')} aria-label="Dismiss notification">×</button></div>}{screen === 'create' ? <DeliveryForm form={form} setForm={setForm} onSubmit={create} onCancel={() => setScreen('deliveries')} /> : <><section className="mb-7 grid gap-4 sm:grid-cols-3"><Metric label="Active deliveries" value={active} hint="Awaiting pickup or in transit" color="text-blue-600" /><Metric label="Delivered" value={delivered} hint="Successfully completed" color="text-emerald-600" /><Metric label="Needs attention" value={attention} hint="Failed delivery attempts" color="text-rose-600" /></section><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]"><section className="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-slate-200"><div className="border-b border-slate-100 p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><h2 className="font-bold">All deliveries <span className="ml-1 text-sm font-medium text-slate-400">({visible.length})</span></h2><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search customer or ID" className="rounded-lg border border-slate-300 px-3 py-2 text-sm outline-none focus:border-blue-500" /></div><div className="mt-4 flex gap-2 overflow-x-auto pb-1">{['ALL', 'NEW', 'ASSIGNED', 'PICKED_UP', 'DELIVERED', 'DELIVERY_FAILED'].map((item) => <button key={item} onClick={() => setFilter(item)} className={`whitespace-nowrap rounded-lg px-3 py-1.5 text-xs font-semibold ${filter === item ? 'bg-slate-900 text-white' : 'bg-slate-100 text-slate-600 hover:bg-slate-200'}`}>{item === 'ALL' ? 'All' : pretty(item)}</button>)}</div></div><div className="divide-y divide-slate-100">{visible.map((d) => <button key={d.id} onClick={() => setSelected(d)} className={`grid w-full grid-cols-[minmax(0,1fr)_auto] gap-3 p-5 text-left transition hover:bg-slate-50 sm:grid-cols-[1.4fr_1fr_auto] ${selected?.id === d.id ? 'bg-blue-50/60' : ''}`}><div><p className="font-semibold">{d.customerName}</p><p className="mt-1 text-sm text-slate-500">{d.address}</p><p className="mt-2 text-xs font-medium text-slate-400">{d.id} · {d.createdAt}</p></div><p className="hidden self-center text-sm text-slate-500 sm:block">{d.itemDescription}</p><div className="self-center text-right"><Status value={d.status} /><p className="mt-2 text-xs font-semibold text-blue-600">View details →</p></div></button>)}{!visible.length && <p className="p-10 text-center text-sm text-slate-500">No deliveries match your filters.</p>}</div></section><Details delivery={selected} /></div></>}</main></div>
}

function Metric({ label, value, hint, color }) { return <div className="rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-sm font-medium text-slate-500">{label}</p><p className={`mt-2 text-3xl font-bold ${color}`}>{value}</p><p className="mt-1 text-xs text-slate-400">{hint}</p></div> }
function DeliveryForm({ form, setForm, onSubmit, onCancel }) { const fields = [['customerName', 'Customer name', 'e.g. Amina Wanjiku'], ['phone', 'Phone number', 'e.g. 0712 345 678']]; return <form onSubmit={onSubmit} className="max-w-3xl rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 sm:p-8"><div className="mb-7"><h2 className="text-xl font-bold">New delivery request</h2><p className="mt-1 text-sm text-slate-500">Add the delivery details. Your dispatcher will assign a rider.</p></div><div className="grid gap-5 sm:grid-cols-2">{fields.map(([name, label, placeholder]) => <label key={name} className="block text-sm font-semibold text-slate-700">{label}<input required value={form[name]} placeholder={placeholder} onChange={(e) => setForm({ ...form, [name]: e.target.value })} className="mt-2 w-full rounded-xl border border-slate-300 px-3.5 py-3 font-normal outline-none placeholder:text-slate-400 focus:border-blue-500 focus:ring-4 focus:ring-blue-100" /></label>)}<TextArea label="Delivery address" name="address" placeholder="Building, street, area, city" rows="2" form={form} setForm={setForm} /><TextArea label="Item description" name="itemDescription" placeholder="Describe what the rider will collect and deliver" rows="3" form={form} setForm={setForm} /></div><div className="mt-7 flex justify-end gap-3 border-t border-slate-100 pt-6"><button type="button" onClick={onCancel} className="rounded-xl px-5 py-3 text-sm font-semibold text-slate-600 hover:bg-slate-100">Cancel</button><button className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-semibold text-white hover:bg-blue-700">Create delivery</button></div></form> }
function TextArea({ label, name, placeholder, rows, form, setForm }) { return <label className="block text-sm font-semibold text-slate-700 sm:col-span-2">{label}<textarea required rows={rows} value={form[name]} placeholder={placeholder} onChange={(e) => setForm({ ...form, [name]: e.target.value })} className="mt-2 w-full resize-none rounded-xl border border-slate-300 px-3.5 py-3 font-normal outline-none placeholder:text-slate-400 focus:border-blue-500 focus:ring-4 focus:ring-blue-100" /></label> }
function Details({ delivery }) { if (!delivery) return <aside className="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200">Select a delivery to view its details.</aside>; return <aside className="h-fit rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200 xl:sticky xl:top-6"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold tracking-wider text-slate-400">DELIVERY DETAILS</p><h2 className="mt-1 text-lg font-bold">{delivery.id}</h2></div><Status value={delivery.status} /></div><div className="mt-6 space-y-4 border-y border-slate-100 py-5"><Info label="Customer">{delivery.customerName}<br /><span className="font-normal text-slate-500">{delivery.phone}</span></Info><Info label="Delivery address">{delivery.address}</Info><Info label="Items">{delivery.itemDescription}</Info>{delivery.failureReason && <div className="rounded-lg bg-rose-50 p-3 text-sm text-rose-800"><strong>Failure reason:</strong> {delivery.failureReason}</div>}</div><h3 className="mt-6 text-sm font-bold">Delivery timeline</h3><ol className="mt-4 space-y-4 border-l-2 border-slate-100 pl-5">{delivery.timeline.map(([event, time], i) => <li key={`${event}-${time}`} className="relative"><span className={`absolute -left-[29px] top-1 h-3 w-3 rounded-full ring-4 ring-white ${i === delivery.timeline.length - 1 ? 'bg-blue-600' : 'bg-slate-300'}`} /><p className="text-sm font-medium text-slate-800">{event}</p><p className="mt-0.5 text-xs text-slate-400">{time}</p></li>)}</ol></aside> }
function Info({ label, children }) { return <div><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-1 text-sm leading-6 text-slate-700">{children}</p></div> }
export default App
